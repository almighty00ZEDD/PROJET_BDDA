Don't forget:

1.Buffer maager replacement policy is working but it's not quite a clock one .... fix it
2.Remove the test parts inside the efficient code (print functions and shit)
3.Get back to every tp to do thing like we should (affectation in the main ...)